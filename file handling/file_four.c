#include<stdio.h>
#include<string.h>
#include<stdlib.h>

#define one 120
#define two 120

void main(){
int arr[one][two];
int tor=0;
FILE* fptr;
char fname[30]="test.txt";
int i;

fptr=fopen(test.txt,"r");
while(arr[i],two,fptr){
arr[i][strlen(arr[i]-1)='\0';
i++;
}
fclose(fptr);
tor=i;
for(i=0;i<tor;i++){
fprintf("%s\n",arr[i]);
}
}
